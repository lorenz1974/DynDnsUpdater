{
  "name": "dyndnsupdater",
  "private": true,
  "version": "0.4.16",
  "releaseDate": "20260328-1012",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "update-release-date": "node ./updateReleaseDate.js",
    "release": "npm run update-release-date && standard-version && git push --follow-tags origin",
    "release:patch": "npm run update-release-date && standard-version --release-as minor && git push --follow-tags origin",
    "release:bugfix": "npm run update-release-date && standard-version --release-as patch && git push --follow-tags origin",
    "push": "git push --follow-tags origin"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "react": "^19.2.4",
    "react-bootstrap": "^2.10.10",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.22.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "standard-version": "^9.5.0",
    "vite": "^8.0.3"
  }
}